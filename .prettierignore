package-lock.json
pnpm-lock.yaml
yarn.lock

# Vendored copy-paste code from the pdfcn and shadcn registries. Reformatting it would destroy the
# ability to diff against upstream, which is the whole reason CLAUDE.md forbids hand-editing it.
# The compatibility shims we own live alongside it and are formatted normally.
src/components/pdf/VENDORED.md

# Generated by the TanStack router CLI on every route change.
src/routeTree.gen.ts

# Machine-written quality output, regenerated by the accuracy suite on every run.
accuracy-report.txt
