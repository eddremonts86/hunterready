# Values docker-compose.yml reads. Copy to `.env` for local use; in Coolify these are the env rows.
# Never commit the filled-in version.

POSTGRES_PASSWORD=change-me
DATABASE_APP_PASSWORD=change-me-too
# Encryption at rest for stored CV content (ADR-021). `openssl rand -hex 32` — exactly 64 hex chars.
# Unset means CVs are stored in plaintext; the app announces which at startup and /privacy reports it.
# LOSING THIS KEY LOSES EVERY STORED CV. Back it up somewhere that is not this server.
DATA_ENCRYPTION_KEY=

BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3100
# Coolify only. Tells its proxy which service and port back the domain; locally the app is reached
# through 0.0.0.0:3100 or by adding a ports mapping in an override file.
SERVICE_FQDN_APP_3000=

# The local model. Declining the third-party provider routes here, so this is not optional in
# spirit — without it, "no" degrades to a rule engine.
OLLAMA_MODEL=qwen2.5:3b-instruct

# The third-party provider, used only with explicit consent.
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.io/anthropic
MINIMAX_MODEL=

# Suspends the *plan* half of the third-party gate: everybody gets MiniMax, signed in or not
# (ADR-030). Consent is NOT suspended — the gate simply starts appearing, because there is now a real
# transfer to agree to.
#
# Production runs with this on. Unset here, the credential above is still loaded but no anonymous
# visitor is entitled to it, so `/api/processing` reports `provider: null`, no consent gate appears,
# and every request quietly uses the local model instead — local behaving unlike production with
# nothing on screen to say so. That is worth knowing before debugging a model you are not using.
#
# ⚠️ On means every ingest, advert read and rewrite spends third-party tokens.
HR_THIRD_PARTY_FOR_ALL=

# ── Beta: everything included, everything still labelled ──────────────────────────────────────────
#
# The product is in beta and nothing can be bought, so a gate in front of a checkout that does not
# exist only costs people the feature. Every Pro capability is included: the larger model, all 103
# designs, your own typefaces and colours, and CVs saved between visits.
#
# **The label is the other half and it is not optional.** Each of those surfaces keeps a Pro tag
# while it is free, drawn from the capability rather than from the entitlement, so a beta user is
# never surprised later by a removal. See `betaPaidFree` in `src/lib/entitlements.ts`.
#
# Defaults ON in code, so leaving this unset is beta. `false` is the only value that ends it, and it
# goes false the day pricing opens — the last item before v1.0.
HR_BETA_PAID_FREE=

# ── The waiting screen's narration ────────────────────────────────────────────────────────────────
#
# The model call is the longest part of an upload, and the only signal inside it is the reasoning
# channel: measured against MiniMax, the tool JSON arrives in a single delta at the very end, while
# `thinking` produces ~200 updates spread over the first half of the wait (src/structure/ask.ts).
#
# The reasoning text never leaves the process — `src/structure/reasoning.ts` matches it against a fixed
# table of our own words and emits a section key, because a model reasoning about a CV reasons in
# somebody's employers and dates and `/api/progress` is unauthenticated by design.
#
# On by default, and it is free: measured A/B on 2026-08-17 the gateway's own run-to-run variance was
# several times larger than the channel's cost. Set to `off` if a provider bills reasoning tokens
# painfully; the screen falls back to a stage list with no detail inside the long row.
HR_REASONING=

# Tokens the model may spend reasoning. Default 3072. The measured run stopped on its own at well under
# that, so this bounds the pathological case rather than setting a target.
HR_REASONING_BUDGET=

# ── A second third-party model, for comparing one against the other ───────────────────────────────
#
# DeepSeek publishes an Anthropic-compatible endpoint (`https://api.deepseek.com/anthropic`) for use
# with Claude Code, which is why `provider.ts` can speak to it with the client it already has.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=
DEEPSEEK_MODEL=

# Which provider answers when several are configured: `deepseek`, `minimax` or `anthropic`.
#
# This is the switch to use when the question is "does this one read a CV better than that one".
# Comparing two models by commenting out a credential is how you end up measuring the wrong one, and a
# name that resolves to nothing here returns *no provider* rather than quietly falling back to another
# — `/api/processing` reports which one answered, so a mistake shows on screen instead of in the data.
HR_PROVIDER=

# ── The switch out of beta ─────────────────────────────────────────────────────────────────────────
#
# `true` and the product is released: the word "beta" leaves the interface, the paid designs and the
# mixed axes are gated, and an anonymous visitor's CV is read on our own hardware. It OVERRIDES
# `HR_BETA_PAID_FREE`, `HR_THIRD_PARTY_FOR_ALL` and `HR_UNLOCK_DESIGNS` rather than defaulting them
# off, so it is a complete answer on its own and cannot be defeated by a variable nobody deleted
# (ADR-033).
#
# It is the same lever as pricing seen from the other side, so it goes true the day the three below
# are set and a test-mode payment has been watched through checkout — not before.
HR_RELEASE=

# ── Payments ──────────────────────────────────────────────────────────────────────────────────────
#
# Absent is a supported state and the normal one until pricing opens: the app boots clean and the
# pricing section says paid plans are not open. `hasCheckout()` needs the key *and* the price, because
# one without the other is a button that 503s.
#
# No card details touch this codebase. The checkout is a hosted Stripe page and the card is typed
# there (src/lib/stripe.ts).
STRIPE_SECRET_KEY=

# The webhook's signing secret, from the endpoint's own page in the Stripe dashboard — not the API key
# and not interchangeable with it. Without it `/api/billing/webhook` refuses every delivery, which is
# the safe direction: an unverified webhook is an unauthenticated request that moves `plan`.
STRIPE_WEBHOOK_SECRET=

# The Stripe Price the subscription charges, and the authority for the amount. `src/lib/pricing.ts`
# holds the display copy of it — €12/month — and the dashboard holds the one that is billed. Change
# the price there first and the constant second.
HR_STRIPE_PRICE_ID=
