/**
 * COMPATIBILITY SHIM — see src/components/pdf/VENDORED.md.
 *
 * `pdf/pdfcn-theme.ts` imports `professionalTheme` from the bare `@/components` path.
 */
export { professionalTheme } from './pdf/theme-professional'
